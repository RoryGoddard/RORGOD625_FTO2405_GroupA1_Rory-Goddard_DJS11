import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    podcasts: [],
}

const podcastSlice = createSlice({
    name: "podcasts",
    initialState,
    reducers: {
        
    }
})